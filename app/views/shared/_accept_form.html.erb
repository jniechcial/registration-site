<%= form_for(user.relationships.find_by(team_id: team.id), :html => { :class => 'form-accept-request', :id => "accept_request_#{user.id}" } ) do |f| %>
  <%= f.submit "Accept", class: "btn btn-mini btn-primary", id: "accept_request_button_#{team.id}" %>
<% end %>